# Vercel Deployment Requirements
# ===============================
# Lightweight dependencies for serverless deployment
# Heavy ML dependencies (torch, whisper, tts) are excluded

fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
PyJWT>=2.8.0
httpx>=0.25.0
pydantic>=2.0.0
